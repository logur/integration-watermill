linters-settings:
    golint:
        min-confidence: 0.1
    goimports:
        local-prefixes: logur.dev/integration/watermill

linters:
    enable-all: true
    disable:
        - funlen
        - gomnd
        - testpackage
